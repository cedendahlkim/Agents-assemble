FROM node:22-alpine

RUN apk add --no-cache python3 make g++

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

# Default command (overridden per service in docker-compose)
CMD ["npm", "run", "start:orchestration"]
